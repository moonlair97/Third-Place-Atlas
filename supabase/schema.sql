create table if not exists public.places (id text primary key, name text, address text, lat double precision, lng double precision, city text, category text, quiet_level int2, lighting_level int2, outlets_density int2, seating_type text, linger_ok boolean, low_sensory boolean, outdoor_seating boolean, accessible_restroom boolean, open_late boolean, wifi_quality int2, safety_evening int2, inserted_at timestamptz default now());